# EXP-06 — Variance risk premium (toy)

Linked signal: **SIG-009**. Synthetic implied vs realized vol spread; costs on exposure changes.

```bash
python experiments/06_variance_risk_premium/run.py
```

Directional: net Sharpe should fall when costs rise (see `sensitivity` in `results/summary.json`).
